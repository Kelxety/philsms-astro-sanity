---
import Button from "./Button.astro";
---

<section class="py-12 sm:py-8 md:py-12 lg:py-14 xl:py-12 2xl:py-28 flex justify-center items-center h-screen w-full">
  <div></div>
  <style>
    #check-icon {
      stroke-dasharray: 450;
      stroke-dashoffset: -30;
      animation: draw 2s linear normal;
    }

    @keyframes draw {
      to {
        stroke-dashoffset: 200;
      }
    }
  </style>
</section>
